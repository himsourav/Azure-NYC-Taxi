In this project NYC Taxi data is fetched dynamically and stored in to Azure DataLake Storage using Azure DataFactory.
Then the data is transformed and loaded into Delta Lake by using Data Bricks while maintaining Medallion Architecture(Bronze -> Silver -> Gold)
